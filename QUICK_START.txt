╔══════════════════════════════════════════════════════════════════╗
║                   GO BACKEND - QUICK START                        ║
╚══════════════════════════════════════════════════════════════════╝

📦 INSTALL DEPENDENCIES
   cd school-go
   go mod download

🚀 RUN THE SERVER
   go run cmd/api/main.go
   
   Server starts at: http://localhost:8080

🧪 TEST IT
   curl http://localhost:8080/health
   curl http://localhost:8080/api/v1/schools

📚 DOCUMENTATION
   README.md          → Full documentation
   GETTING_STARTED.md → Beginner-friendly guide  ⭐ START HERE
   EXAMPLES.md        → Code examples & recipes
   ARCHITECTURE.md    → System design explained

🔧 CUSTOMIZE FOR YOUR PROJECT
   1. Edit: internal/fetcher/school_fetcher.go
      → Add your actual data sources (APIs, CSV, etc.)
   
   2. Update: internal/models/school.go
      → Adjust fields to match your data
   
   3. Modify: .env
      → Change schedule, port, database path

📋 AVAILABLE COMMANDS
   make help          → Show all commands
   make run           → Run the server
   make build         → Build binary
   make test          → Run tests

🎯 KEY FILES TO UNDERSTAND (in order)
   1. cmd/api/main.go              → See how everything connects
   2. internal/models/school.go    → Data structures
   3. internal/handler/school_handler.go → HTTP endpoints
   4. internal/service/school_service.go → Business logic
   5. internal/repository/school_repository.go → Database

🌐 API ENDPOINTS
   GET    /health                  → Health check
   GET    /api/v1/schools          → Get all schools
   GET    /api/v1/schools/:id      → Get one school
   POST   /api/v1/schools          → Create school
   PUT    /api/v1/schools/:id      → Update school
   DELETE /api/v1/schools/:id      → Delete school
   POST   /api/v1/refresh          → Manual data refresh

🔄 PROJECT STRUCTURE
   cmd/           → Entry points (main.go files)
   internal/      → Your application code
   ├── config/    → Configuration
   ├── database/  → Database setup
   ├── models/    → Data structures
   ├── repository/→ Database queries
   ├── service/   → Business logic
   ├── fetcher/   → External data fetching
   ├── handler/   → HTTP handlers
   ├── scheduler/ → Cron jobs
   └── server/    → HTTP server setup

💡 TIPS
   • Copy .env.example to .env (or it's already created)
   • The scheduler runs daily at 2 AM (configurable in .env)
   • SQLite database created automatically in data/ folder
   • Read GETTING_STARTED.md for detailed explanations

🎓 LEARNING PATH
   Week 1: Understand the structure, run it, test endpoints
   Week 2: Implement your actual data fetchers
   Week 3: Add new models (teachers, students, etc.)
   Week 4: Add tests, authentication, more features

🆘 TROUBLESHOOTING
   Port already in use?
   → Change PORT in .env or kill process: lsof -ti:8080 | xargs kill
   
   Import errors?
   → Run: go mod tidy
   
   Database locked?
   → Close other connections, restart server

📚 GO RESOURCES
   https://gobyexample.com/
   https://go.dev/tour/
   https://go.dev/doc/effective_go

═══════════════════════════════════════════════════════════════════
                      You're all set! 🎉
═══════════════════════════════════════════════════════════════════


